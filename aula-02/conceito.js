console.log("=== CONCEITO.JS ===");

// Camada de Armazenamento (Varíaveis)
// chave = valor;
let nome = "Leonardo";
let idade = 40;
const alto = true;

// Camada de Instruções (Comandos)
// comando();
window.alert();

// comando(param);
confirm("Você é maior de idade?");

// comando(param1, param2);
nome = prompt("Digite seu nome:", "Seu nome aqui");

// contexto.comando();
console.log();

// contexto.comando(param);
console.log(nome);

// contexto.comando(param1, param2);
console.log("idade", idade);

